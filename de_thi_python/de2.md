<p style="margin:0.25px;text-align:center; font-weight:bold;">TRƯỜNG ĐẠI HỌC ĐÔNG Á</p>
<p style="margin:0.25px;text-align:center; font-weight:bold;">KHOA ĐIỆN - ĐIỆN TỬ</p>
<hr>
<p style="margin:0.25px;text-align:center; font-weight:bold;">ĐỀ KIỂM TRA GIỮA KỲ</p>
<p style="margin:0.25px;text-align:center; font-weight:bold;">Mã đề :02</p>
<p style="margin:0.25px;text-align:center; font-weight:bold;">Môn thi: Lập trình python</p>
<p style="margin:0.25px;text-align:center; font-weight:bold;">Thời gian làm bài : 120 phút</p>
<p style="margin:0.25px;text-align:center; font-weight:bold;">HÌnh thức: Trắc nghiệm và tự luận</p> 
<p style="margin:0.25px;text-align:center;">(Sinh viên được phép dùng tài liệu)</p>
<hr>

# Phần 1: Trắc nghiệm (4 điểm)

1.  Trong Python, câu lệnh `while n < 5:` sẽ lặp lại khối lệnh bên trong chừng nào điều kiện nào còn đúng?   
a) `n` bằng 5   
b) `n` lớn hơn hoặc bằng 5  
c) `n` nhỏ hơn 5    
d) `n` khác 5
2.  Kiểu dữ liệu nào trong Python được dùng để lưu trữ số nguyên?   
a) Float    
b) Boolean  
c) Int  
d) String
3.  Để lấy tổng số phần tử trong một danh sách (List) trong Python, ta dùng hàm nào?    
a) `count()`    
b) `sum()`  
c) `len()`  
d)`size()`  
4.  Nguyên tắc cốt lõi của **Tính Đa hình (Polymorphism)** là gì?   
a) Gom tất cả thuộc tính và phương thức vào một lớp.    
b) Tái sử dụng code từ Lớp Cha sang Lớp Con.    
c) Một hàm hoặc đối tượng có thể xử lý nhiều kiểu đối tượng khác nhau như thể chúng là một.   
d) Không thể thay đổi phương thức của Lớp Cha.

5.  Trong một vòng lặp `for`, từ khóa `continue` có tác dụng gì?    
a) Thoát khỏi vòng lặp ngay lập tức.    
b) Bỏ qua các dòng code còn lại trong vòng lặp hiện tại và chuyển sang lần lặp (iteration) tiếp theo.   
c) Báo lỗi và dừng chương trình.    
d) Quay lại lần lặp trước đó.   

6.  Nếu bạn muốn chuyển một chuỗi ký tự thành định dạng **tất cả đều viết hoa (ALL CAPS)**, bạn sử dụng phương thức nào?    
a) `title()`    
b) `capitalize()`   
c) `upper()`    
d) `strip()`
7.  Trong lập trình Python, đâu là vai trò của **tham số (parameter)** trong định nghĩa hàm?      
a) Để chạy hàm.     
b) Là biến để nhận thông tin đầu vào khi hàm được gọi.  
c) Để định nghĩa giá trị trả về.    
d) Là từ khóa để đặt tên hàm.
8.  Sự khác nhau cơ bản giữa Shallow copy và Deep copy đối với List là gì?      
a) Shallow copy nhanh hơn Deep copy.    
b) Deep copy tạo bản sao List mới, các phần tử con cũng được sao chép sang vùng nhớ mới; Shallow copy chỉ sao chép tham chiếu (reference) đến đối tượng bên trong List gốc.   
c) Shallow copy chỉ dùng cho List, Deep copy dùng cho Dictionary.   
d) Shallow copy là bản sao sâu, Deep copy là bản sao nông.

9.  Để in ra phần tử ở vị trí **thứ hai** trong List `fruits = ["Cherry", "Apple", "Pear"]`, bạn sử dụng cú pháp nào?   
a) `print(fruits[2])`   
b) `print(fruits[1])`     
c) `print(fruits[0])`   
d)`print(fruits.get(1))`

10.  Cho đoạn code: `s = " hello world "; s = s.strip().upper(); print(s)`. Kết quả in ra là gì?    
a) `hello world`    
b) `HELLO WORLD`    
c) `HELLO WORLD`    
d) `Hello World`    

11.  Câu lệnh nào của SQL được sử dụng để **thêm** dữ liệu vào một bảng trong CSDL SQLite?      
a) `SELECT`     
b) `UPDATE`     
c) `DELETE`     
d) `INSERT`     
12.  Kết quả của phép toán Python sau là gì: `7 // 3`?  
a) 2.3333   
b) 2    
c) 1    
d) 7/3
13.  Cho dictionary `data = {"temp": 30.5, "hum": 75}`. Sau khi chạy `data["press"] = 1010`, số lượng cặp Key-Value trong `data` là bao nhiêu?  
a) 2    
b) 3    
c) 4    
d) Lỗi
14.  Giả sử bạn cần mô phỏng kết quả của trò chơi **Kéo, Búa, Bao** (với 0 là Kéo, 1 là Búa, 2 là Bao), bạn nên dùng phương thức nào của module `random`?   
a) `random.uniform(0, 2)`   
b) `random.randint(0, 2)`   
c) `random.choice([0, 1, 2])`   
d) `random.random()`

15.  Tại sao khi thực thi lệnh SQL trong Python, ta nên dùng **placeholder (?)** với tham số (ví dụ: `WHERE id = ?`) thay vì dùng f-string để chèn trực tiếp giá trị vào chuỗi SQL?     
a) Để tăng tốc độ thực thi.     
b) Để chống lại lỗi cú pháp.    
c) Để ngăn chặn lỗi **SQL Injection**, bảo mật dữ liệu.     
d) Placeholder chỉ dùng cho lệnh `SELECT`.
16.  Nếu bạn muốn lặp qua cả **Key và Value** của một Dictionary cùng một lúc, phương thức nào được coi là hiệu quả nhất để sử dụng trong vòng lặp `for`?   
a) Duyệt Key mặc định: `for key in dict:`   
b) Duyệt Value: `for value in dict.values():`   
c) Duyệt Key-Value: `for key, value in dict.items():`   
d) Duyệt Index:`for i in range(len(dict)):`
17.  Nguyên lý **Tính Kế thừa (Inheritance)** trong OOP có mục đích chính là gì?    
a) Để đảm bảo Lớp Con không thể thay đổi thuộc tính của Lớp Cha.    
b) Để tái sử dụng code, tránh lặp lại các thuộc tính và phương thức chung ở nhiều lớp khác nhau.    
c) Để định nghĩa một lớp duy nhất cho toàn bộ hệ thống.     
d) Để ẩn các phương thức không cần thiết.
18.  Khi bạn cần lấy nhiệt độ ( $298.15$ ) từ dữ liệu API lồng nhau sau, cú pháp nào là chính xác: `weather_response_data = {"main": {"temp": 298.15, "humidity": 75}}`     
a) `weather_response_data.main.temp`    
b) `weather_response_data['main']['temp']` 
c) `weather_response_data.get('temp')`  
d) `weather_response_data['temp']`  
19.  Việc chuyển đổi kết quả truy vấn CSDL (dạng `tuple` thô) thành một danh sách các đối tượng (như `SensorData` Class) mang lại lợi ích gì cho lập trình viên? a) Tăng tốc độ chương trình. b) Giúp code dễ đọc, dễ bảo trì hơn vì tránh việc truy cập bằng chỉ số khó hiểu (magic number) như `data[1]`. ✔ c) Làm cho dữ liệu trở thành bất biến (Immutable). d) Đó là quy tắc bắt buộc của thư viện `sqlite3`.
20.  Câu lệnh nào dưới đây dùng để **tạo ra dãy số nguyên 1, 3, 5, 7, 9**?  
a) `range(1, 10)`   
b) `range(1, 10, 1)`    
c) `range(1, 10, 2)`    
d) `range(1, 9, 2)`


# Phần 2: Tự luận (6 điểm)

**Bài 1 (3 điểm)**  
Viết hàm Python có tên là `tinh_tip_hoa_don` nhận vào 3 tham số: **tổng tiền hóa đơn** (`bill_amount`), **tỉ lệ tip** (`tip_percentage`, ví dụ: 10, 15, 20), và **số người chia** (`num_people`).

Hàm có nhiệm vụ:

1.  Tính tổng tiền phải thanh toán, bao gồm cả tiền tip.
2.  Tính số tiền mỗi người cần trả.
3.  Hàm **trả về** (dùng `return`) số tiền mỗi người cần trả (làm tròn đến 2 chữ số thập phân).
4.  Gọi hàm với dữ liệu mẫu:
    *   Tổng tiền hóa đơn: ** $150$ **
    *   Tỉ lệ Tip: ** $15%$ **
    *   Số người: ** $5$ **
5.  In kết quả ra màn hình với thông báo rõ ràng.

**Bài 2 (3 điểm)**
Dựa trên kiến thức về OOP, hãy thực hiện các yêu cầu sau:
1.  **Định nghĩa Lớp Cha `Car`:**
    *   Khởi tạo (`__init__`) với 3 thuộc tính: `brand` (hãng xe), `color` (màu sắc), và `max_speed` (tốc độ tối đa, km/h).
    *   Định nghĩa phương thức `get_info()`: Trả về chuỗi thông tin: `"<brand> - Màu <color> - Tốc độ tối đa: <max_speed> km/h"`
2.  **Định nghĩa Lớp Con `ElectricCar`:**
    *   Kế thừa từ lớp `Car`.
    *   Thêm thuộc tính mới: `battery_capacity` (dung lượng pin, kWh).
    *   Ghi đè phương thức khởi tạo (`__init__`) và sử dụng `super()` để truyền `brand`, `color`, `max_speed` lên Lớp Cha. Lớp `ElectricCar` sẽ nhận **4 tham số**.
    *   Ghi đè phương thức `get_info()` (override) để trả về thông tin chuyên biệt: `"ElectricCar: <brand> - Pin: <battery_capacity> kWh"`.
3.  **Kiểm tra:**
    *   Tạo đối tượng `my_ev` với dữ liệu: `brand='Tesla'`, `color='White'`, `max_speed=250`, `battery_capacity=100`.
    *   In kết quả trả về từ `my_ev.get_info()` ra màn hình

